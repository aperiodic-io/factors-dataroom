# Vendored Aperiodic Factors API client (see ./aperiodic-factors-client).
# Provides the importable `aperiodic` package; not published to PyPI.
./aperiodic-factors-client
matplotlib
pandas
numpy
requests
alphalens-reloaded
seaborn
joblib
python-dotenv
jupytext
nbconvert
ipykernel
# finml-utils[plotting] would pin matplotlib==3.9.2; install plain + the only
# missing transitive dep (tabulate is used by the reports module that
# quantstats/__init__.py eagerly imports, so we need it even though we only
# touch the stats functions).
finml-utils
tabulate
pyyaml